<!DOCTYPE html>
<html lang="EN">
    <head>
        <%- include('./partials/head')%>
        <title>Music box</title>
        <link rel="stylesheet" href="/css/register.css"> 
    </head>
    <body>
        <%- include('./partials/header')%>
        <main>
            <form action="/users/" class="caja" method="POST" enctype="multipart/form-data">
               
                <div class="formulario"> 
                    <h1>Formulario de registro</h1>
                    
                    <div class="form">
                        <input class="barra" type="text" name="nombre" id="nombre" placeholder="Nombre" value="<%= locals.oldData ? oldData.nombre : null %>" >
                        <% if (locals.errors && errors.nombre) { %>
                            <div class="text-danger">
                                <%= errors.nombre.msg %> 
                            </div>
                        <% } %>
                    </div>
                    <div class="form">
                        <input class="barra" type="text" name="nick" id="nick" placeholder="Nombre de usuario" value="<%= locals.oldData ? oldData.nick : null %>">
                        <% if (locals.errors && errors.nick) { %>
                            <div class="text-danger">
                                <%= errors.nick.msg %> 
                            </div>
                        <% } %>
                    </div>
                    <div class="form">
                        <input class="barra" type="tel" name="dni" id="dni" placeholder="DNI" value="<%= locals.oldData ? oldData.dni : null %>">
                        <% if (locals.errors && errors.dni) { %>
                            <div class="text-danger">
                                <%= errors.dni.msg %> 
                            </div>
                        <% } %>
                    </div>  
                    <div class="form">
                        <input class="barra" type="password" name="pass" id="pass" placeholder="Contraseña">
                        <% if (locals.errors && errors.pass) { %>
                            <div class="text-danger">
                                <%= errors.pass.msg %> 
                            </div>
                        <% } %>
                    </div>
                    <div class="form">
                        <input class="barra" type="email" name="email" id="email" placeholder="Email" value="<%= locals.oldData ? oldData.email : null %>">
                        <% if (locals.errors && errors.email) { %>
                            <div class="text-danger">
                                <%= errors.email.msg %> 
                            </div>
                        <% } %>
                        
                    </div>
                    <div class="form">
                        <input class="barra" type="password" name="repass" id="repass" placeholder="Repetir Contraseña">
                        <% if (locals.errors && errors.repass) { %>
                            <div class="text-danger">
                                <%= errors.repass.msg %> 
                            </div>
                        <% } %>
                    </div>

                    <div class="form">
                        <input class="barra" type="file" name="userImage" id="user-image" accept=".jpg, .png">
                        <% if (locals.errors && errors.userImage) { %>
                            <div class="text-danger">
                                <%= errors.userImage.msg %> 
                            </div>
                        <% } %>
                    </div>
                    
                    <div class="terminos, hl">
                        <input type="checkbox" name="terminos" id="TyC" required>
                        <label for="TyC">Acepto los terminos y condiciones</label>
                    </div>
                    <div class="terminos">
                        <input type="checkbox" name="terminos" id="TyC" >
                        <label for="TyC">deseo recibir notificaciones a mi email acerca de actualizaciones 
                    <div class="botones">
                        <button type="reset">Borrar</button>
                        <button type="submit">Aceptar</button>
                    </div>
                    <div id="formFooter">
                        <a class="Hover" href="/login">Ya estás registrado? Inicia sesión</a>
                    </div>
                </div>
            </form>
        </main>
        <%- include('./partials/footer')%> 
    </body>
</html>